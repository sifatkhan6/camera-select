import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <div>
            <h1 className='hearer'>Goriber Camera Shop</h1>
        </div>
    );
};

export default Header;